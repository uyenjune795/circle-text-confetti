//instantiate new object
let confetti = new Confetti ('button-container');
    /*variable*/ /*library*/

//options
confetti.destroyTarget(false);
confetti.setSize(1.5);

import CircleType from "https://cdn.skypack.dev/circletype@2.3.0";

// Instantiate `CircleType` with an HTML element.
//const circleType = new CircleType(document.getElementById('text'));

// Set the text radius and direction. Note: setter methods are chainable.
//circleType.radius(100).dir(1);

const circleType = new CircleType(document.getElementById('text'));